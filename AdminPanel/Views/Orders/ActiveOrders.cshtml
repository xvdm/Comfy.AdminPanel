@model IEnumerable<Order>
@{
    ViewData["Title"] = "Active orders";
}

<div class="Page">
    <div class="header">
        <div class="namePage">
            <p>Активні замовлення</p>
        </div>
    </div>

    <div class="body body-with-sections">

        <div class="section">
            <div class="search-block">
                <input class="search-block-input" type="text" name="SearchString" />
                <button class="search-block-btn" type="submit" value="Пошук">
                    <span class="material-icons "> search </span>
                </button>
            </div>
        </div>

@*        <div class="sections">
            <div class="products-list-block">
                @if (Model is not null)
                {
                    <div>
                        <table class="table">
                            <thead class="thead-dark">
                                <tr>
                                    <th scope="col">Дата замовленння</th>
                                    <th scope="col">Статус</th>
                                    <th scope="col">Опис</th>
                                </tr>
                            </thead>
                            <tbody class="productsTable">
                                @foreach (var order in Model.Where(x => x.Status.Id == 1))
                                {
                                    <tr>
                                        <td>@order.CreatingDate</td>
                                        <td>@order.Status.Status</td>
                                        <td>@order.Description</td>
                                    </tr>
                                }
                            </tbody>
                        </table>

                    </div>
                }
            </div>
        </div>*@

        <div class="sections">
            <div class="pagination-block">
            </div>
        </div>

    </div>
</div>